#include<iostream>
#include<vector>
#include"room.h"

vector map_generator
{
  /*Push back 20 rooms with constructor with parameters. Use drawn out map to hard code each room number with
    appropriate north,south,east,west values.
    Main Program for application can use accessor functions to set and "move" to current room in map.
    Using numerical value of -1 as a  NULL room choice. Since there are 20 rooms, there are indices of 0-19. -1 will be     used to check condition and so it will tell you if this is a valid option or not.int Room::get_east()
{
  return east;
}

*/
  
  return dungeon;
}
